# MSX ROM dumps — NEVER commit copyrighted game data
*.rom
*.ROM
*.mx1
*.mx2
*.dsk
*.cas
Juegos/
roms/

# Build artifacts
*.sym
*.o
__pycache__/
*.pyc
*.pyo
.mypy_cache/
.pytest_cache/

# Test images / scratch files
test_*.rom
all_*.rom

# User-specific TOML configs that reference local ROM paths
all_*.toml
test_*.toml

# Editor / OS
.DS_Store
*.swp
.vscode/
.idea/

# forja local dev tooling (forensic gate config + reports)
.forja/

# Allow the pre-built launcher (it's our own code, not a ROM)
!launcher/launcher.bin

# build intermediates
testroms/*.raw
